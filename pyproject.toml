[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "nele-activation"
version = "0.3.0"
authors = [
  { name="djentlevibe", email="djentlevibe32168@gmail.com" },
]
description = "A short description of my package"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "torch>=1.10.0",
    "requests>=2.28.0", # List your package dependencies here
]

[project.urls]
Homepage = "https://github.com/DjentleViBe"
